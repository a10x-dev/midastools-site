# Conversion-integrity sweep of the proven-buyer funnel

Purpose: When held/saturated with flat revenue, audit the live buy-CTA surfaces the ACTUAL buyer traverses for trust-leaks instead of holding or over-shipping

When to use: , the day's distinct content unit is already shipped, and every revenue lever is human/calendar-gated. Instead of a 4th grounding-and-hold OR a 2nd content over-ship, this is a genuinely-new autonomous, reversible, bottleneck-direct lever — sweeping the commercial surfaces the PROVEN buyer actually crosses. Fires when severity is "conversion" and 100% of revenue comes from a funnel you can inspect at source.

---

When to use: Metrics are flat, the day's distinct content unit is already shipped, and every revenue lever is human/calendar-gated. Instead of a 4th grounding-and-hold OR a 2nd content over-ship, this is a genuinely-new autonomous, reversible, bottleneck-direct lever — sweeping the commercial surfaces the PROVEN buyer actually crosses. Fires when severity is "conversion" and 100% of revenue comes from a funnel you can inspect at source.
---
1. Identify the proven-buyer funnel from customer-attribution data (here: off-list Stripe-Link business-pack buyers → gist/blog → Mega Pack $29 / Bundle $97 → checkout). Do NOT sweep the non-converting audience's surfaces (art treadmill).
2. Read the live commercial-intent pages end-to-end (not grep-by-pattern): every buy-CTA, kit-count claim, price/discount math, prompt-count claim, and any dead-tool diversion box sitting at peak intent.
3. For each numeric/kit-count claim, verify against the DESTINATION page's canonical framing (`verify-product-claims-at-source`). Flag ONLY per-page self-contradictions (buyer sees two values in one glance) or overstatements that contradict the destination — those are yours to fix. Cross-page naming variance (9 vs 13 vs 16 vs 15) is the reserved Armando strategic call; do NOT mass-edit it.
4. grep the whole codebase to confirm the flagged claim is the ONLY instance of its class before editing — surgical, not spray.
5. Fix one class, build clean, push, live-verify the fix serves, then STOP. Don't chain-find a 3rd task to look busy — genuine saturation after one qualifying fix.
6. Held-not-fixed items (bundle-aggregate counts, strategic naming) get logged for the Armando queue, not edited.
